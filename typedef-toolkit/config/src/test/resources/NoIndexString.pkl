amends "modulepath:/CustomAtlanModel.pkl"

t = "NoIndexString"
a = "nis"

supertype {
  groupDefinition {
    name = t
    description = "No Index String"
    superTypes { "Catalog" }
    attributeDefs {
      new {
        name = a
        description = a
        type = "string"
      }
    }
  }
}
