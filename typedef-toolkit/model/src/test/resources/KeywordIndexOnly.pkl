amends "modulepath:/CustomAtlanModel.pkl"

t = "KeywordIndexOnly"
a = "kio"

supertype {
  groupDefinition {
    name = t
    description = "Keyword Index Only"
    superTypes { "Catalog" }
    attributeDefs {
      new {
        name = a
        description = a
        type = "string"
        indexAs = "keyword"
      }
    }
  }
}
