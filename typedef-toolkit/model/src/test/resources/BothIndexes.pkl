amends "modulepath:/CustomAtlanModel.pkl"

t = "BothIndexes"
a = "bi"

shared {
  supertypeDefinition {
    name = t
    description = "Both Indexes"
    superTypes { "Catalog" }
    attributeDefs {
      new {
        name = a
        description = a
        type = "string"
        indexAs = "both"
      }
    }
  }
}
