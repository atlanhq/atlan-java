amends "modulepath:/Model.pkl"

t = "BothIndexes"
a = "bi"

shared {
  supertypeDefinition {
    name = t
    description = "Both Indexes"
    superTypes { "Catalog" }
    attributes {
      [a] {
        description = a
        type = "string"
        indexAs = "both"
      }
    }
  }
}
