amends "modulepath:/Model.pkl"

t = "TextIndexOnly"
a = "tio"

shared {
  supertypeDefinition {
    name = t
    description = "Text Index Only"
    superTypes { "Catalog" }
    attributes {
      [a] {
        description = a
        type = "string"
        indexAs = "text"
      }
    }
  }
}
