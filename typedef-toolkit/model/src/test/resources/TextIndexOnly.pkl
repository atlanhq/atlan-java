amends "modulepath:/CustomAtlanModel.pkl"

t = "TextIndexOnly"
a = "tio"

supertype {
  groupDefinition {
    name = t
    description = "Text Index Only"
    superTypes { "Catalog" }
    attributeDefs {
      new {
        name = a
        description = a
        type = "string"
        indexAs = "text"
      }
    }
  }
}
