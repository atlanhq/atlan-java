amends "modulepath:/Model.pkl"

t = "NoIndex"
a = "ni"

shared {
  supertypeDefinition {
    name = t
    description = "No Index"
    superTypes { "Catalog" }
    attributes {
      [a] {
        description = a
        type = "string"
        multiValued = true
      }
    }
  }
}
