amends "modulepath:/Model.pkl"

t = "NoIndexNonString"
a = "nins"

shared {
  supertypeDefinition {
    name = t
    description = "No Index Non-String"
    superTypes { "Catalog" }
    attributes {
      [a] {
        description = a
        type = "long"
      }
    }
  }
}
