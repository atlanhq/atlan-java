amends "modulepath:/CustomAtlanModel.pkl"

t = "NoIndexNonString"
a = "nins"

supertype {
  groupDefinition {
    name = t
    description = "No Index Non-String"
    superTypes { "Catalog" }
    attributeDefs {
      new {
        name = a
        description = a
        type = "int"
      }
    }
  }
}
